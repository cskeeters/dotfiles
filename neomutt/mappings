# vim: filetype=neomuttrc

## LEADERS

bind index,pager g noop       # Disable to support gg, gi, etc
# bind index,pager <Space> noop # overrides collapse-thread
bind index,pager , noop

## ACCOUNTS

macro index,pager ,aw '<sync-mailbox><enter-command>source ~/.config/neomutt/accounts/gmail.work<enter><change-folder>!<enter>'
macro index,pager ,ak '<sync-mailbox><enter-command>source ~/.config/neomutt/accounts/gmail.karta<enter><change-folder>!<enter>'
macro index,pager ,ao  '<sync-mailbox><enter-command>source ~/.config/neomutt/accounts/outlook-personal<enter><change-folder>!<enter>'
macro index,pager ,ay  '<sync-mailbox><enter-command>source ~/.config/neomutt/accounts/yahoo<enter><change-folder>!<enter>'
# macro index,pager ,al '<sync-mailbox><enter-command>source ~/.config/neomutt/accounts/local<enter><change-folder>!<enter>'

## NAVIGATION

# --- Index (Message List) Navigation ---
# bind index   j        next-entry
# bind index   k        previous-entry
bind pager   j        next-line
bind pager   k        previous-line
bind browser h        goto-parent
bind browser l        select-entry


bind index   gg       first-entry
bind index   G        last-entry
bind pager   gg       top
bind pager   G        bottom
bind browser gg       top-page
bind browser G        bottom-page

bind index,pager \Cd half-down     # Ctrl-d
bind index,pager \Cu half-up       # Ctrl-u
bind index,pager \Cf next-page     # Ctrl-f
bind index,pager \Cb previous-page # Ctrl-b
# bind index,pager,browser d half-down
# bind index,pager,browser u half-up

bind index \031 previous-undeleted  # Mouse wheel
bind index \005 next-undeleted      # Mouse wheel
bind pager \031 previous-line       # Mouse wheel
bind pager \005 next-line           # Mouse wheel


## INDEX

### Viewing

macro index,pager ,r  "<mark-message>z<enter><change-folder>^<enter>'z" # refresh, maintain cursor

bind index ,l limit           # Additional Filter

### Processing

# global index and pager shortcuts
# bind index,pager D purge-message
macro index,pager D "<delete-message><next-entry>" "Delete and move next" # May be overriden by accounts using notmuch

# bind index "#" noop # Get rid of break-thread binding
# bind index "#" delete-message # Use D
macro index,pager U "<undelete-message><next-entry>" "Undelete and move next" # Overrides <undelete-pattern>

# bind index <tab> sync-mailbox

# Quickly open w3m
macro index,pager V "<view-attachments><search>html<enter><view-mailcap><exit>" "Open HTML Alternative"


### Actions

# r reply # Default
bind index,pager R group-reply      # Reply All

bind index,pager ,m compose-to-sender


# Press A to add contact to Khard address book
macro index,pager A \
    "<pipe-message>khard add-email<return>" \
    "add the sender email address to khard"


## PAGER

macro pager ,p "<enter-command>unalternative_order *; alternative_order text/plain text/html<enter><exit><display-message>" "Prefer Text"
macro pager ,h "<enter-command>unalternative_order *; alternative_order text/html text/plain<enter><exit><display-message>" "Prefer Html"

### URLS

# This is not needed with yy in w3m ~/.w3m/keymap
# macro pager \cb "<pipe-message>python3 -m quopri -d | grep -oE 'https?://[^ >\"]+' | fzf | osccopy<enter>" "Extract URLs to fzf and copy via OSC52"
# macro pager \cb "<pipe-message>python3 -m quopri -d | mimehtml | /Users/chad/bin/htmla | fzf -d '\t' --accept-nth 2 > url<Enter><shell-escape>cat url<Enter>" "Select and Copy URL to Clipboard (via OSC52)"

# Unused, using w3m
# macro index,pager \cb "<pipe-message> urlscan<Enter>" "call urlscan to extract URLs out of a message"
# macro attach,compose \cb "<pipe-entry> urlscan<Enter>" "call urlscan to extract URLs out of a message"


## COMPOSE

bind attach <return> view-mailcap
# bind attach l view-mailcap

### Markdown

macro compose Y "<pipe-entry>~/bin/mutt_markdown.sh<Enter>\
<attach-file>/tmp/email.txt<Enter>\
<tag-entry>\
<toggle-disposition>\
<attach-file>/tmp/email.html<Enter>\
<tag-entry>\
<toggle-disposition>\
<view-attach>\
<attach-file>/tmp/email.md<Enter>\
<tag-entry>\
<toggle-disposition>\
<edit-type><home><kill-line>text/markdown; charset=utf-8<enter>\
<group-alternatives>" "Convert Markdown to HTML/Plain Multipart"


## SIDEBAR
bind index,pager \Ck sidebar-prev
bind index,pager \Cj sidebar-next
bind index,pager \Co sidebar-open
bind index,pager \Cp sidebar-prev-new
bind index,pager \Cn sidebar-next-new
bind index,pager B sidebar-toggle-visible


## LINE EDITOR (To: Bindings)
bind editor <space> noop
bind editor <Tab> complete-query
bind editor ^T complete


## IMAP

bind pager ,u imap-fetch-mail
